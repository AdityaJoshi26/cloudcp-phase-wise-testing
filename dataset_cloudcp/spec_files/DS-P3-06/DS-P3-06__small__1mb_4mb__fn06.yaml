# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P3-06  (Phase 3 — Batch Exhaustion / Weight Shift)
# Name      : Only Tiny Remains
# Tier      : small   |   Filename variant: FN-06
# Size band : 1mb_4mb   |   Files in THIS spec: 51
# Seal cfg  : max_files=512, target_bytes=2GB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# NOTE: exact count not divisible for the requested tree depth (3); using flat mode (single dir) to keep the count exact.
# Exhaust-first tier: tiny (last standing). Profile dt2_100gbe (6:4:3:3).
# Plan Master Summary lists 400,584 (tiers only); +4,000 zero-byte seeds = 404,584 total.
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: flat
root: /bryck/cloudcp/DS-P3-06/small/1mb_4mb/fn06
threads: 16
seed: 1554267835

content:
  type: random
  buffer_size: 8MB
  direct_io: { enabled: false }
  fsync: false

naming:
  charset: mixed
  alphabet: [lower, digit]
  unicode_blocks:
    - latin-supplement
  special_chars: "_"
  length: 16
  collision_strategy: append-index

flat:
  num_files: 51

size:
  type: range
  min: 1048576
  max: 4194304
  distribution: uniform

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: "", weight: 1 }
