# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P11-02  (Phase 11 — Alternative Weight Ratios)
# Name      : Weights 9:5:2:0 (~5 TB)
# Tier      : large   |   Filename variant: FN-09
# Size band : 10gb_20gb   |   Files in THIS spec: 9
# Seal cfg  : max_files=8, target_bytes=50GB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# NOTE: exact count not divisible for the requested tree depth (3); using flat mode (single dir) to keep the count exact.
# Scheduler weights large:9 medium:5 small:2 tiny:0 (tiny intentionally empty).
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: flat
root: /bryck/cloudcp/DS-P11-02/large/10gb_20gb/fn09
threads: 16
seed: 318650911

content:
  type: random
  buffer_size: 8MB
  direct_io:
    enabled: true
    min_size: 256MB
  fsync: false

naming:
  charset: ascii
  alphabet: [lower, digit]
  length: 14
  collision_strategy: append-index

flat:
  num_files: 9

size:
  type: range
  min: 10737418240
  max: 21474836480
  distribution: uniform

extensions:
  - { ext: .tar.gz, weight: 1 }
  - { ext: .tar.bz2, weight: 1 }
  - { ext: "", weight: 1 }
