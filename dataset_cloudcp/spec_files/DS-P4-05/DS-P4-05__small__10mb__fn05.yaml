# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P4-05  (Phase 4 — Filename & Encoding Stress)
# Name      : Filename Stress: Cross-Tier
# Tier      : small   |   Filename variant: FN-05
# Size band : 10mb   |   Files in THIS spec: 100
# Seal cfg  : max_files=512, target_bytes=2GB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: tree
root: /bryck/cloudcp/DS-P4-05/small/10mb/fn05
threads: 16
seed: 807737930

content:
  type: random
  buffer_size: 8MB
  direct_io: { enabled: false }
  fsync: false

naming:
  charset: ascii
  alphabet: [lower, digit]
  suffix: "\r"
  length: 14
  collision_strategy: append-index

tree:
  fanout: 2
  depth: 2
  files_per_dir: 25
  files_in_each_dir: false

size:
  type: fixed
  bytes: 10485760

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: "", weight: 1 }
