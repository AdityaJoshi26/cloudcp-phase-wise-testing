# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P4-05  (Phase 4 — Filename & Encoding Stress)
# Name      : Filename Stress: Cross-Tier
# Tier      : medium   |   Filename variant: FN-02
# Size band : 200mb   |   Files in THIS spec: 20
# Seal cfg  : max_files=64, target_bytes=10GB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: tree
root: /bryck/cloudcp/DS-P4-05/medium/200mb/fn02
threads: 16
seed: 1315331375

content:
  type: random
  buffer_size: 8MB
  direct_io: { enabled: false }
  fsync: false

naming:
  charset: ascii
  alphabet: [lower, digit]
  special_chars: " "
  length: 16
  collision_strategy: append-index

tree:
  fanout: 2
  depth: 2
  files_per_dir: 5
  files_in_each_dir: false

size:
  type: fixed
  bytes: 209715200

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: "", weight: 1 }
