# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P2-01  (Phase 2 — Batch Builder Mechanics)
# Name      : Boundary Values Probe
# Tier      : tiny   |   Filename variant: FN-04
# Size band : 10kb   |   Files in THIS spec: 1
# Seal cfg  : max_files=2000, target_bytes=256MB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# Each size band's prefix encodes the size (e.g. 'boundary_64mb_').
# 10 files per boundary, one per FN-01..FN-10.
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: flat
root: /bryck/cloudcp/DS-P2-01/tiny/10kb/fn04
threads: 16
seed: 387572185

content:
  type: random
  buffer_size: 8MB
  direct_io: { enabled: false }
  fsync: false

naming:
  charset: ascii
  alphabet: [lower, digit]
  special_chars: "\n"
  prefix: "boundary_10kb_"
  length: 16
  collision_strategy: append-index

flat:
  num_files: 1

size:
  type: fixed
  bytes: 10240

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: "", weight: 1 }
