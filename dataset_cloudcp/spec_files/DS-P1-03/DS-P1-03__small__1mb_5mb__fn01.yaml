# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P1-03  (Phase 1 — Single-Tier Isolation)
# Name      : Small-Tier Pure (~5 TB)
# Tier      : small   |   Filename variant: FN-01
# Size band : 1mb_5mb   |   Files in THIS spec: 3,000
# Seal cfg  : max_files=512, target_bytes=2GB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# NOTE: exact count not divisible for the requested tree depth (4); using flat mode (single dir) to keep the count exact.
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: flat
root: /bryck/cloudcp/DS-P1-03/small/1mb_5mb/fn01
threads: 16
seed: 1174615998

content:
  type: random
  buffer_size: 8MB
  direct_io: { enabled: false }
  fsync: false

naming:
  charset: ascii
  alphabet: [lower, upper, digit, dash]
  length: 16
  collision_strategy: append-index

flat:
  num_files: 3000

size:
  type: range
  min: 1048576
  max: 5242880
  distribution: uniform

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: "", weight: 1 }
