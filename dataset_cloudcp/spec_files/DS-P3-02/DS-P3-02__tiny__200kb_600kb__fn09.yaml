# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P3-02  (Phase 3 — Batch Exhaustion / Weight Shift)
# Name      : Medium Exhausts First
# Tier      : tiny   |   Filename variant: FN-09
# Size band : 200kb_600kb   |   Files in THIS spec: 6,000
# Seal cfg  : max_files=2000, target_bytes=256MB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# Exhaust-first tier: medium. Profile dt2_100gbe (6:4:3:3).
# Plan Master Summary lists 75,916 (tiers only); +600 zero-byte seeds = 76,516 total.
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: tree
root: /bryck/cloudcp/DS-P3-02/tiny/200kb_600kb/fn09
threads: 16
seed: 1376438290

content:
  type: random
  buffer_size: 8MB
  direct_io: { enabled: false }
  fsync: false

naming:
  charset: ascii
  alphabet: [lower, digit]
  length: 14
  collision_strategy: append-index

tree:
  fanout: 2
  depth: 3
  files_per_dir: 750
  files_in_each_dir: false

size:
  type: range
  min: 204800
  max: 614400
  distribution: uniform

extensions:
  - { ext: .tar.gz, weight: 1 }
  - { ext: .tar.bz2, weight: 1 }
  - { ext: "", weight: 1 }
