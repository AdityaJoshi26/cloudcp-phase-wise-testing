# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P7-01  (Phase 7 — Mixed Full-Pipeline)
# Name      : Mixed Pipeline: Small (~300 GB)
# Tier      : large   |   Filename variant: FN-06
# Size band : 5gb_12gb   |   Files in THIS spec: 2
# Seal cfg  : max_files=8, target_bytes=50GB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# NOTE: exact count not divisible for the requested tree depth (3); using flat mode (single dir) to keep the count exact.
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: flat
root: /bryck/cloudcp/DS-P7-01/large/5gb_12gb/fn06
threads: 16
seed: 80565019

content:
  type: random
  buffer_size: 8MB
  direct_io:
    enabled: true
    min_size: 256MB
  fsync: false

naming:
  charset: mixed
  alphabet: [lower, digit]
  unicode_blocks:
    - latin-supplement
  special_chars: "_"
  length: 16
  collision_strategy: append-index

flat:
  num_files: 2

size:
  type: range
  min: 5368709120
  max: 12884901888
  distribution: uniform

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: "", weight: 1 }
