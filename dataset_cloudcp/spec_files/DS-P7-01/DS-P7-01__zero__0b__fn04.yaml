# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P7-01  (Phase 7 — Mixed Full-Pipeline)
# Name      : Mixed Pipeline: Small (~300 GB)
# Tier      : zero   |   Filename variant: FN-04
# Size band : 0b   |   Files in THIS spec: 500
# Seal cfg  : no batching (zero bucket)
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: tree
root: /bryck/cloudcp/DS-P7-01/zero/0b/fn04
threads: 16
seed: 1857465581

content:
  type: sparse

naming:
  charset: ascii
  alphabet: [lower, digit]
  special_chars: "\n"
  length: 16
  collision_strategy: append-index

tree:
  fanout: 5
  depth: 3
  files_per_dir: 4
  files_in_each_dir: false

size:
  type: fixed
  bytes: 0

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: "", weight: 1 }
