# ════════════════════════════════════════════════════════════════════════════
# Dataset   : DS-P10-07  (Phase 10 — Sub-Range Isolation)
# Name      : 10GB-120GB large 30
# Tier      : large   |   Filename variant: FN-12
# Size band : 10gb_120gb   |   Files in THIS spec: 1
# Seal cfg  : max_files=8, target_bytes=50GB, open_batches=8
# Profile   : dt2_100gbe
# ────────────────────────────────────────────────────────────────────────────
# Generated by generate_specs.py from dataset_generation_plan.md.
# One spec per (size-band x filename-variant): datagen supports only
# ONE naming policy per spec, so this is how exact per-variant counts
# are achieved. File TYPES are sampled from the weighted `extensions`
# catalogue below (per-type share is therefore APPROXIMATE).
# ────────────────────────────────────────────────────────────────────────────
# FN-12 APPROX: shell metacharacters injected via special_chars pool; exact placement not guaranteed.
# ════════════════════════════════════════════════════════════════════════════

version: 1
mode: flat
root: /bryck/cloudcp/DS-P10-07/large/10gb_120gb/fn12
threads: 16
seed: 2041645378

content:
  type: random
  buffer_size: 8MB
  direct_io:
    enabled: true
    min_size: 256MB
  fsync: false

naming:
  charset: ascii
  alphabet: [lower, digit]
  special_chars: "$!&;|"
  length: 14
  collision_strategy: append-index

flat:
  num_files: 1

size:
  type: range
  min: 10737418240
  max: 128849018880
  distribution: uniform

extensions:
  - { ext: .csv, weight: 1 }
  - { ext: .json, weight: 1 }
  - { ext: .txt, weight: 1 }
  - { ext: .log, weight: 1 }
  - { ext: .sql, weight: 1 }
  - { ext: .xml, weight: 1 }
  - { ext: .yaml, weight: 1 }
  - { ext: .bin, weight: 1 }
  - { ext: .gz, weight: 1 }
  - { ext: .tar, weight: 1 }
  - { ext: .zip, weight: 1 }
  - { ext: .zst, weight: 1 }
  - { ext: .bz2, weight: 1 }
  - { ext: .7z, weight: 1 }
  - { ext: .parquet, weight: 1 }
  - { ext: .avro, weight: 1 }
  - { ext: .orc, weight: 1 }
  - { ext: .arrow, weight: 1 }
  - { ext: .hdf5, weight: 1 }
  - { ext: .jpg, weight: 1 }
  - { ext: .png, weight: 1 }
  - { ext: .mp4, weight: 1 }
  - { ext: .mkv, weight: 1 }
  - { ext: .wav, weight: 1 }
  - { ext: .so, weight: 1 }
  - { ext: "", weight: 1 }
